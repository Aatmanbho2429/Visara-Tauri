# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Frozen sidecar staged for `bundle.externalBin`, named
# pictoria-sidecar-<target-triple>[.exe]. ~200MB, rebuilt by PyInstaller in CI
# (and locally by the command in sidecar/README.md) — never committed.
/binaries/
